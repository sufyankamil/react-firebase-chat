import React from "react";
import { render, screen } from "@testing-library/react";
import List from "./List";

test("renders List component", () => {
  render(<List />);
  // Add specific assertions based on the content of your List component
});
